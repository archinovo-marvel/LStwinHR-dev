# 企业端面试审查报告剩余待办清单

> 整理日期：2026-05-08
> 来源：企业端面试访谈模块审查报告.md

## 1. 说明

本清单只保留“截至当前仍未彻底完成”或“仅部分完成”的条目。

已经明显推进的项目，例如：

- useInterview 抽离
- 竞态串行化
- 错误边界
- 恢复提示
- 数字人挂载与桥接

不再重复列为原始待办。

## 2. 高优先级剩余项

### 2.1 彻底移除 VirtualHumanSDK 内部旧面试状态机

当前状态：已基本完成

- 对外旧 API 已收缩。
- useSDKConnection 中对旧 interviewState 的直接读写已移除。
- useInterviewState.js 已删除。

剩余动作：

- 继续审视是否还存在旧面试语义命名或兼容包装。

### 2.2 企业端面试会话后端增量同步

当前状态：checkpoint + 周期心跳 + 恢复态对齐第二版已落地

剩余动作：

- 页面级验证语音草稿、恢复成功后的消息回放与输入区联动是否符合预期。

已补充验证：

- token 缺失跳过同步但流程继续。
- PATCH 失败不阻断答题推进。
- 心跳 checkpoint 只发送 metadata，不携带 conversation。
- checkRecoverableSession 会先比较恢复进度，再在进度相同时比较同步时间。
- ChatPage recovery banner 已覆盖挂载检查、恢复按钮与放弃按钮交互。

### 2.3 InterviewPage 最终清理

当前状态：已完成

说明：

- 废弃 InterviewPage.js 已删除。
- 后续仅需保持文档与引用不回退。

## 3. 中优先级剩余项

### 3.1 语音回答闭环

当前状态：第二版已落地

剩余动作：

- SDK 原生 ASR 转写结果已接入 useVirtualHumanBridge，并复用现有 voice draft 通道。
- 语音回答状态已从 ChatPage 下沉到 bridge。
- 页面级验证自动提交后消息回放、加载态与异常提示是否符合预期。

已补充验证：

- useVirtualHumanBridge.asr.test.jsx 已覆盖 sdk-asr transcript 上抛与诊断事件记录。
- InputBar.voice-answer.test.jsx 已覆盖语音提交策略开关切换。
- ChatPage.voice-submit-mode.test.jsx 已覆盖自动提交路径会走 submitAnswer。
- 自动提交触发点已从 ChatPage effect 下沉到 bridge 回调，页面层桥接负担进一步下降。

### 3.2 问题播报完成后的状态边界

当前状态：未严格建模

剩余动作：

- 补 QUESTION_PRESENTED 或等价事件。
- 避免问题尚未播报完成就进入等待回答态。

### 3.3 数字人桥接层继续收敛

当前状态：已有 useVirtualHumanBridge 雏形

剩余动作：

- 把更多副作用从 ChatPage 下沉到 bridge。
- 将诊断、音频控制、播报状态进一步标准化。

## 4. 低优先级或长期项

### 4.1 interviewScoring.js 继续拆分

当前状态：未开始

说明：

- 这是可维护性问题，但不是当前面试主链路稳定性的首阻碍。

### 4.2 图片上传与 chat API 的进一步安全治理

当前状态：部分改善

说明：

- 客户端已有压缩，但仍可继续做更严格的大小控制和服务端限制。

### 4.3 chat 相关接口认证与资源保护

当前状态：未在本轮处理

说明：

- 更偏服务端安全治理，可另开一轮。

## 5. 推荐后续执行顺序

建议按以下顺序继续：

1. 为语音回答闭环补更多页面级验证与策略开关。
2. 细化恢复态冲突处理与页面级恢复体验。
3. 之后继续收敛 ChatPage 中剩余桥接代码。
4. 之后再继续收敛 ChatPage 中剩余桥接代码。
